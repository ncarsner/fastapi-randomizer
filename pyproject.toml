[project]
name = "fast-api-demo"
version = "0.1.0"
description = "FastAPI-driven web application to generate random selections"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.121.3",
]
